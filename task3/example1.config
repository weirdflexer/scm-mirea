def PORT := 5432

server -> {
    database -> mydb.
    user -> admin.
    port -> @[PORT].
    host -> localhost.
    alpha -> {
        database -> mydb.
    }
    beta -> {
        database -> mydb.
    }
}